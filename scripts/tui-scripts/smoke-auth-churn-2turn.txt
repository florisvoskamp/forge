# Live smoke: two turns in ONE mesh-unpinned session. Turn 1 may auth-fail a keyless provider
# (e.g. opencode_go::glm-5.2) — with the v0.3.1 fix that model is EXCLUDED, so turn 2 must NOT
# retry it (no second "auth failed" failover hop). Confirms the failover-churn fix end-to-end.
sleep 4000
expect message
send Write hello.py that prints "hi" and run it with python3. Then say done.
sleep 90000
capture turn1
send Now write bye.py that prints "bye" and run it with python3. Then say done.
sleep 90000
capture turn2
reject panic
reject <invoke
send /quit
